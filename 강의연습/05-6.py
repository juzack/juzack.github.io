'''표준 라이브러리
파이썬 설치 시 자동으로 포함. import 사용해야 함.
datetime.date 연, 월, 일로 날짜를 표현할 때 사용하는 함수
time
    time.time()
    time.localtime()
math
random
    random.random
    random.int
    p263 - random_pop.py
shutil 파일 복사 이동 모듈
glob 특정 디렉터리 안의 파일 이름을 읽ㅇ서 리스트로 리턴 / *, ? 등 메타 문자 사용 가능
pickle 객체 형태 유지하며 파일에 저장하고 불러올 수 있게 하는모듈
    pickle.dump
    pickle.loaad
os 환경 변수나 디렉터리, 파일 등의 os 자원을 제어할 수있게 해 주는 모듈
    os.chdir
    os.hetcwd
    os.system
    os.mkdir(directory)
    os.rmdir(directory)
    os.remove(파일)
    os.rename(src, dst)
json json 데이터를 쉽게 처리하고자 사용하는 모듈

'''